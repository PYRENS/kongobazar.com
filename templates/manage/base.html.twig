<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Back-Office{% endblock %} — KongoBazar</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flag-icons@7.2.3/css/flag-icons.min.css">
    <link rel="stylesheet" href="{{ asset('styles/manage/admin.css') }}">
    {% block stylesheets %}{% endblock %}
</head>
<body class="admin-body{{ app.user and app.user.adminDarkMode ? ' admin-dark' : '' }}"
      {% if app.user and app.user.adminSidebarColor %}style="--admin-sidebar-color: {{ app.user.adminSidebarColor }};"{% endif %}>

    <nav class="admin-sidebar">
        <div class="admin-sidebar-brand">
            <span class="logo-kongo">Kongo</span><span class="logo-bazar">Bazar</span>
            <span class="admin-sidebar-tag">Admin</span>
        </div>
        <ul class="admin-nav">
            <li class="{{ app.request.attributes.get('_route') == 'manage_dashboard' ? 'active' : '' }}">
                <a href="{{ path('manage_dashboard') }}"><i class="bi bi-speedometer2"></i> Tableau de bord</a>
            </li>
            <li class="{{ app.request.attributes.get('_route') starts with 'manage_users' ? 'active' : '' }}">
                <a href="{{ path('manage_users_index') }}"><i class="bi bi-people"></i> Utilisateurs</a>
            </li>
            <li class="{{ app.request.attributes.get('_route') starts with 'manage_geo' ? 'active' : '' }}">
                <a href="{{ path('manage_geo_index') }}"><i class="bi bi-geo-alt"></i> Entités admin.</a>
            </li>
            <li class="{{ app.request.attributes.get('_route') starts with 'manage_categories' ? 'active' : '' }}">
                <a href="{{ path('manage_categories_index') }}"><i class="bi bi-tags"></i> Catégories</a>
            </li>
            <li class="{{ app.request.attributes.get('_route') starts with 'manage_trending' ? 'active' : '' }}">
                <a href="{{ path('manage_trending_index') }}"><i class="bi bi-graph-up-arrow"></i> Tendances</a>
            </li>
            <li class="{{ app.request.attributes.get('_route') starts with 'manage_mega_menu' ? 'active' : '' }}">
                <a href="{{ path('manage_mega_menu_index') }}"><i class="bi bi-grid-3x3-gap"></i> Méga-menu (header)</a>
            </li>
            <li class="{{ app.request.attributes.get('_route') starts with 'manage_top_category' ? 'active' : '' }}">
                <a href="{{ path('manage_top_category_index') }}"><i class="bi bi-star"></i> Top Catégorie</a>
            </li>
            <li class="{{ app.request.attributes.get('_route') starts with 'manage_brands' ? 'active' : '' }}">
                <a href="{{ path('manage_brands_index') }}"><i class="bi bi-award"></i> Marques</a>
            </li>
            {% set autoMotoRoutes = ['manage_vehicle_models', 'manage_vehicle_variants', 'manage_vehicle_engines', 'manage_fuel_types', 'manage_vehicle_import'] %}
            {% set autoMotoRefTypes = ['permis', 'types-moto'] %}
            {% set autoMotoActive = false %}
            {% for r in autoMotoRoutes %}
                {% if app.request.attributes.get('_route') starts with r %}{% set autoMotoActive = true %}{% endif %}
            {% endfor %}
            {% if app.request.attributes.get('_route') starts with 'manage_reference' and app.request.get('type') in autoMotoRefTypes %}{% set autoMotoActive = true %}{% endif %}

            <li class="admin-nav-group {{ autoMotoActive ? 'open' : '' }}">
                <button type="button" class="admin-nav-group-toggle">
                    <i class="bi bi-car-front"></i> Auto-Moto <i class="bi bi-chevron-down admin-nav-group-arrow"></i>
                </button>
                <ul class="admin-nav-submenu">
                    <li class="{{ app.request.attributes.get('_route') starts with 'manage_vehicle_models' ? 'active' : '' }}">
                        <a href="{{ path('manage_vehicle_models_index') }}"><i class="bi bi-car-front"></i> Modèles véhicules</a>
                    </li>
                    <li class="{{ app.request.attributes.get('_route') starts with 'manage_vehicle_variants' ? 'active' : '' }}">
                        <a href="{{ path('manage_vehicle_variants_index') }}"><i class="bi bi-diagram-3"></i> Variantes véhicules</a>
                    </li>
                    <li class="{{ app.request.attributes.get('_route') starts with 'manage_vehicle_engines' ? 'active' : '' }}">
                        <a href="{{ path('manage_vehicle_engines_index') }}"><i class="bi bi-gear"></i> Motorisations</a>
                    </li>
                    <li class="{{ app.request.attributes.get('_route') starts with 'manage_fuel_types' ? 'active' : '' }}">
                        <a href="{{ path('manage_fuel_types_index') }}"><i class="bi bi-fuel-pump"></i> Énergies</a>
                    </li>
                    <li class="{{ app.request.attributes.get('_route') starts with 'manage_reference' and app.request.get('type') == 'permis' ? 'active' : '' }}">
                        <a href="{{ path('manage_reference_index', {type: 'permis'}) }}"><i class="bi bi-card-checklist"></i> Permis requis</a>
                    </li>
                    <li class="{{ app.request.attributes.get('_route') starts with 'manage_reference' and app.request.get('type') == 'types-moto' ? 'active' : '' }}">
                        <a href="{{ path('manage_reference_index', {type: 'types-moto'}) }}"><i class="bi bi-scooter"></i> Types de moto</a>
                    </li>
                    <li class="{{ app.request.attributes.get('_route') starts with 'manage_vehicle_import' ? 'active' : '' }}">
                        <a href="{{ path('manage_vehicle_import') }}"><i class="bi bi-upload"></i> Import motorisations</a>
                    </li>
                    <li class="{{ app.request.attributes.get('_route') starts with 'manage_vehicle_search' ? 'active' : '' }}">
                        <a href="{{ path('manage_vehicle_search') }}"><i class="bi bi-search"></i> Recherche</a>
                    </li>
                    <li class="{{ app.request.attributes.get('_route') starts with 'manage_part_catalog' ? 'active' : '' }}">
                        <a href="{{ path('manage_part_catalog_index') }}"><i class="bi bi-collection"></i> Catalogue Pièces</a>
                    </li>
                </ul>
            </li>

            <li class="{{ app.request.attributes.get('_route') starts with 'manage_reference' and app.request.get('type') == 'periodes-location' ? 'active' : '' }}">
                <a href="{{ path('manage_reference_index', {type: 'periodes-location'}) }}"><i class="bi bi-calendar-range"></i> Périodes de location</a>
            </li>
            <li class="{{ app.request.attributes.get('_route') starts with 'manage_products' ? 'active' : '' }}">
                <a href="{{ path('manage_products_index') }}"><i class="bi bi-box-seam"></i> Produits</a>
            </li>
            <li class="{{ app.request.attributes.get('_route') starts with 'manage_ads' ? 'active' : '' }}">
                <a href="{{ path('manage_ads_index') }}"><i class="bi bi-megaphone"></i> Publicités</a>
            </li>
            <li class="{{ app.request.attributes.get('_route') starts with 'manage_ad_zones' ? 'active' : '' }}">
                <a href="{{ path('manage_ad_zones_index') }}"><i class="bi bi-shuffle"></i> Zones publicitaires</a>
            </li>
            <li>
                <a href="{{ path('manage_seller_attr_test_index') }}"><i class="bi bi-person-gear"></i> Test — Caractéristiques vendeur</a>
            </li>
            <li class="admin-nav-group {{ app.request.attributes.get('_route') starts with 'manage_settings' ? 'open' : '' }}">
                <button type="button" class="admin-nav-group-toggle">
                    <i class="bi bi-gear"></i> Paramètres <i class="bi bi-chevron-down admin-nav-group-arrow"></i>
                </button>
                <ul class="admin-nav-submenu">
                    <li class="{{ app.request.attributes.get('_route') == 'manage_settings' ? 'active' : '' }}">
                        <a href="{{ path('manage_settings') }}"><i class="bi bi-palette"></i> Interface</a>
                    </li>
                </ul>
            </li>
        </ul>
        <div class="admin-sidebar-footer">
            <a href="{{ path('manage_logout') }}"><i class="bi bi-box-arrow-right"></i> Déconnexion</a>
        </div>
    </nav>

    <div class="admin-sidebar-overlay" id="sidebarOverlay"></div>

    <main class="admin-main">
        <header class="admin-topbar">
            <button type="button" class="admin-sidebar-burger" id="sidebarBurgerToggle" aria-label="Ouvrir le menu">
                <i class="bi bi-list"></i>
            </button>
            <h5><i class="bi {% block page_icon %}bi-circle{% endblock %}"></i> {% block page_title %}Tableau de bord{% endblock %}</h5>
            <div class="admin-topbar-user">
                <button type="button" class="admin-darkmode-toggle" id="darkModeQuickToggle" aria-label="Basculer le mode nuit">
                    <i class="bi bi-moon-stars"></i>
                </button>
                {% if app.user %}
                    {% if app.user.avatarName %}
                        <img src="{{ vich_uploader_asset(app.user, 'avatarFile') }}" class="admin-avatar" alt="">
                    {% else %}
                        <span class="admin-avatar admin-avatar--initials">{{ app.user.initials }}</span>
                    {% endif %}
                    {{ app.user.email }}
                {% endif %}
            </div>
        </header>

        <div class="admin-content">
            {% for label, messages in app.flashes %}
                {% set bsClass = label == 'error' ? 'danger' : label %}
                {% for message in messages %}
                    <div class="alert alert-{{ bsClass }}">{{ message }}</div>
                {% endfor %}
            {% endfor %}

            {% block body %}{% endblock %}
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="{{ asset('js/manage/admin.js') }}"></script>
    <script src="{{ asset('js/manage/geo-picker.js') }}"></script>
    <script src="{{ asset('js/manage/category-picker.js') }}"></script>
    <script src="{{ asset('js/manage/vehicle-model-picker.js') }}"></script>
    <script src="{{ asset('js/manage/product-pickers.js') }}"></script>
    <script src="{{ asset('js/manage/part-compatibility.js') }}"></script>
    <script src="{{ asset('js/manage/product-attribute-paste.js') }}"></script>
    <script src="{{ asset('js/manage/product-attribute-duplicate.js') }}"></script>
    <script src="{{ asset('js/manage/seller-attribute-widget.js') }}"></script>
    <script src="{{ asset('js/manage/part-brand-autocomplete.js') }}"></script>
    <script src="{{ asset('js/manage/ad-category-autocomplete.js') }}"></script>
    <script src="{{ asset('js/manage/part-catalog-compatibility.js') }}"></script>
    <script>
        (function () {
            const burger = document.getElementById('sidebarBurgerToggle');
            const sidebar = document.querySelector('.admin-sidebar');
            const overlay = document.getElementById('sidebarOverlay');
            if (!burger || !sidebar || !overlay) return;

            const close = () => {
                sidebar.classList.remove('admin-sidebar--open');
                overlay.classList.remove('admin-sidebar-overlay--visible');
            };
            const toggle = () => {
                sidebar.classList.toggle('admin-sidebar--open');
                overlay.classList.toggle('admin-sidebar-overlay--visible');
            };

            burger.addEventListener('click', toggle);
            overlay.addEventListener('click', close);
        })();
    </script>
    {% block javascripts %}{% endblock %}
</body>
</html>