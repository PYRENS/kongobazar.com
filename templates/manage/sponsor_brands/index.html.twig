{% extends 'manage/base.html.twig' %}
{% block title %}Marques sponsors{% endblock %}
{% block page_icon %}bi-award{% endblock %}
{% block page_title %}Marques sponsors — Accueil{% endblock %}
{% block body %}
    <div class="sw-card mb-3">
        <div class="card-header"><h6 class="mb-0 fw-bold"><i class="bi bi-toggle-on text-primary me-2"></i>Visibilité</h6></div>
        <div class="card-body">
            <div class="form-check form-switch">
                <input type="checkbox" class="form-check-input" id="sponsorSectionEnabledSwitch" role="switch" {{ sectionSetting.enabled ? 'checked' : '' }} data-toggle-url="{{ path('manage_sponsor_brands_toggle_section') }}">
                <label class="form-check-label">Afficher la section "Marques sponsors" sur la page d'accueil</label>
            </div>
        </div>
    </div>

    <div class="sw-card mb-3">
        <div class="card-header"><h6 class="mb-0 fw-bold"><i class="bi bi-plus-circle text-primary me-2"></i>Ajouter</h6></div>
        <div class="card-body">
            <form method="post" action="{{ path('manage_sponsor_brands_add') }}" enctype="multipart/form-data" class="d-flex gap-3 align-items-end flex-wrap">
                <div>
                    <label class="form-label small">Nom</label>
                    <input type="text" name="name" class="form-control" required>
                </div>
                <div>
                    <label class="form-label small">Lien (optionnel)</label>
                    <input type="url" name="url" class="form-control" placeholder="https://...">
                </div>
                <div>
                    <label class="form-label small">Logo</label>
                    <div class="admin-file-upload-group" id="sponsorLogoUploadGroup">
                        <div class="admin-upload-thumb" id="sponsorLogoDropzoneThumb">
                            <span class="admin-upload-thumb-label">&nbsp;</span>
                            <label class="admin-upload-dropzone" for="sponsorLogoInput">
                                <i class="bi bi-camera"></i>
                                <span>Upload</span>
                            </label>
                        </div>
                        <input type="file" name="logo" id="sponsorLogoInput" accept="image/*" class="admin-upload-input-hidden">
                    </div>
                </div>
                <button type="submit" class="btn btn-primary btn-sm">Ajouter</button>
            </form>
        </div>
    </div>

    <div class="sw-card">
        <div class="card-header"><h6 class="mb-0 fw-bold"><i class="bi bi-list-ol text-primary me-2"></i>Marques sponsors configurées</h6></div>
        <div class="card-body p-0">
            <table class="table ctb-table mb-0">
                <thead><tr><th style="width:80px;">Ordre</th><th style="width:80px;">Logo</th><th>Nom</th><th>Lien</th><th style="width:90px;">Actif</th><th style="width:100px;">Actions</th></tr></thead>
                <tbody>
                    {% for sponsor in sponsors %}
                        <tr>
                            <td>
                                <form method="post" action="{{ path('manage_sponsor_brands_move', {id: sponsor.id, direction: 'up'}) }}" style="display:inline;">
                                    <button type="submit" class="admin-btn admin-btn--outline"{{ loop.first ? ' disabled' }}><i class="bi bi-arrow-up"></i></button>
                                </form>
                                <form method="post" action="{{ path('manage_sponsor_brands_move', {id: sponsor.id, direction: 'down'}) }}" style="display:inline;">
                                    <button type="submit" class="admin-btn admin-btn--outline"{{ loop.last ? ' disabled' }}><i class="bi bi-arrow-down"></i></button>
                                </form>
                            </td>
                            <td>{% if sponsor.logoName %}<img src="{{ vich_uploader_asset(sponsor, 'logoFile') }}" style="max-width:50px;max-height:30px;object-fit:contain;">{% endif %}</td>
                            <td>{{ sponsor.name }}</td>
                            <td>{{ sponsor.url ?: '—' }}</td>
                            <td>
                                <input type="checkbox" class="form-check-input sponsor-toggle" role="switch" {{ sponsor.active ? 'checked' : '' }} data-toggle-url="{{ path('manage_sponsor_brands_toggle', {id: sponsor.id}) }}">
                            </td>
                            <td>
                                <form method="post" action="{{ path('manage_sponsor_brands_remove', {id: sponsor.id}) }}" onsubmit="return confirm('Retirer ?');">
                                    <button type="submit" class="btn btn-sm btn-outline-danger">Retirer</button>
                                </form>
                            </td>
                        </tr>
                    {% else %}
                        <tr><td colspan="6" class="text-muted">Aucun élément configuré.</td></tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

    <script>
        (function () {
            const sw = document.getElementById('sponsorSectionEnabledSwitch');
            sw.addEventListener('change', function () {
                const wasChecked = this.checked;
                fetch(this.dataset.toggleUrl, { method: 'POST' })
                    .then((r) => r.json())
                    .then((data) => { this.checked = data.enabled; })
                    .catch(() => { this.checked = !wasChecked; });
            });
        })();

        document.querySelectorAll('.sponsor-toggle').forEach((sw) => {
            sw.addEventListener('change', function () {
                const wasChecked = this.checked;
                fetch(this.dataset.toggleUrl, { method: 'POST' })
                    .then((r) => r.json())
                    .then((data) => { this.checked = data.active; })
                    .catch(() => { this.checked = !wasChecked; });
            });
        });

        const sponsorLogoInput = document.getElementById('sponsorLogoInput');
        const sponsorLogoUploadGroup = document.getElementById('sponsorLogoUploadGroup');
        const sponsorLogoDropzoneThumb = document.getElementById('sponsorLogoDropzoneThumb');

        sponsorLogoInput.addEventListener('change', function () {
            const file = this.files[0];
            sponsorLogoUploadGroup.querySelectorAll('[data-kind="new"]').forEach((el) => el.remove());
            if (!file) return;

            const div = document.createElement('div');
            div.className = 'admin-upload-thumb';
            div.dataset.kind = 'new';
            div.innerHTML =
                '<span class="admin-upload-thumb-label">&nbsp;</span>' +
                '<div class="admin-upload-thumb-inner">' +
                '<img src="' + URL.createObjectURL(file) + '" class="admin-zoomable-thumb">' +
                '<button type="button" class="admin-upload-remove"><i class="bi bi-x"></i></button>' +
                '</div>';
            div.querySelector('.admin-upload-remove').addEventListener('click', () => {
                sponsorLogoInput.value = '';
                div.remove();
            });
            sponsorLogoDropzoneThumb.after(div);
        });
    </script>
{% endblock %}
