{% extends 'manage/base.html.twig' %}

{% block title %}Modifier la Vente Flash{% endblock %}
{% block page_icon %}bi-tag{% endblock %}
{% block page_title %}Modifier la Vente Flash — {{ campaign.product.title }}{% endblock %}

{% block body %}
<div class="admin-card" style="max-width:500px;">
    <form method="post">
        <div class="mb-3">
            <label class="form-label">Produit</label>
            <input type="text" class="form-control" value="{{ campaign.product.title }}" disabled>
        </div>
        <div class="mb-3">
            <label class="form-label">Prix normal actuel</label>
            <input type="text" class="form-control" value="{{ campaign.product.basePrice }} {{ campaign.product.currency }}" disabled>
        </div>
        <div class="mb-3">
            <label class="form-label">Prix remisé</label>
            <input type="number" step="0.01" name="discounted_price" class="form-control" value="{{ campaign.discountedPrice }}" required>
        </div>
        <div class="mb-3">
            <label class="form-label">Début de la remise</label>
            <div class="d-flex gap-3 mb-2">
                <div class="form-check">
                    <input type="radio" name="start_mode" value="now" class="form-check-input" id="startModeNow">
                    <label class="form-check-label" for="startModeNow">Redémarrer maintenant</label>
                </div>
                <div class="form-check">
                    <input type="radio" name="start_mode" value="scheduled" class="form-check-input" id="startModeScheduled" checked>
                    <label class="form-check-label" for="startModeScheduled">Programmée</label>
                </div>
            </div>
            <input type="datetime-local" name="start_at" id="startAtInput" class="form-control" style="max-width:280px;" value="{{ campaign.startAt|date('Y-m-d\\TH:i') }}">
        </div>
        <div class="mb-3">
            <label class="form-label">Fin de la remise</label>
            <input type="datetime-local" name="end_at" class="form-control" value="{{ campaign.endAt|date('Y-m-d\\TH:i') }}" required>
        </div>
        <button type="submit" class="admin-btn admin-btn--primary">Enregistrer</button>
        <a href="{{ path('manage_discount_campaigns_index') }}" class="admin-btn admin-btn--outline">Annuler</a>
    </form>
</div>

<script>
    const startInput = document.querySelector('[name="start_at"]');
    const endInput = document.querySelector('[name="end_at"]');
    endInput.min = startInput.value;
    startInput.addEventListener('change', () => {
        endInput.min = startInput.value;
    });

    document.querySelectorAll('[name="start_mode"]').forEach((radio) => {
        radio.addEventListener('change', function () {
            startInput.disabled = this.value === 'now';
        });
    });
</script>

{% endblock %}